apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "fullname" . }}
  labels:
    app: {{ include "name" . }}
    chart: {{ include "chart" . }}
data:
  config.json: |-
{{ .Values.config | toPrettyJson | indent 4 }}
